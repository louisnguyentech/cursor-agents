Path rules (STRICT):

- All modules MUST be created under:
  src/modules/<module-name>

- Schema definitions live in:
  .ai/schemas/<module>.schema.md

- DTOs MUST be under:
  src/modules/<module>/dto

Never violate these rules.
